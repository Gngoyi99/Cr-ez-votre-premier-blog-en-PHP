<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}{% endblock %}</title>
    <!-- Intégration de Bootstrap CSS via CDN -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    <!-- Lien vers votre fichier de style personnalisé -->
    <link href="style.css" rel="stylesheet">
</head>
<body class="{% if username %}background-logged-in{% else %}background-logged-out{% endif %}">
    <header>
        <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
            <div class="container">
                <a class="navbar-brand" href="/BlogPHP/home">Accueil</a>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse" id="navbarNav">
                    <ul class="navbar-nav ms-auto">
                        {% if username %}
                            <li class="nav-item">
                                <span class="nav-link">Bonjour, {{ username }}</span>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link" href="/BlogPHP/listPost">Tous les articles</a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link" href="/BlogPHP/addPost">Ajouter un article</a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link" href="/BlogPHP/editProfile">Modifier le profil</a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link" href="/BlogPHP/logout">Se déconnecter</a>
                            </li>
                        {% else %}
                            <li class="nav-item">
                                <a class="nav-link" href="/BlogPHP/userLogin">Se connecter</a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link" href="/BlogPHP/userRegister">S'inscrire</a>
                            </li>
                        {% endif %}
                    </ul>
                </div>
            </div>
        </nav>
    </header>

    <div class="container mt-4">
        {% block content %}
        <h1>Bienvenue sur mon site</h1>     
        {% endblock %}
    </div>

    <!-- scripts JS pour le Bootstrap -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
</body>
</html>
